# AGENTS.md

本文件适用于整个仓库。所有 Codex/Agent 后续协作都必须优先遵守这里的项目边界、科学约束和工程规则。

## 项目目标

- 当前阶段只复现原始 SubNetX 的行为、数据流程和示例结果，重点是让基线流程可运行、可检查、可复现。
- 暂时不修改科学算法，包括路径搜索、子网络扩展、收敛规则、反应筛选、热力学评价和排序逻辑。
- 后续目标是在完成基线复现后，开发新的代谢途径发现与排序模型；新模型开发必须与基线复现任务分开提交和验证。

## 仓库结构

- `1_subnetwork_extraction/`: 子网络提取阶段。包含 `code/` 中的主流程、`defaults/` 默认参数、`data/` 反应网络与生物体代谢物数据、`tutorials/` 示例项目，以及 `analyses/` 分析脚本和历史输出。
- `2_subentwork_analysis/`: 子网络分析和路径评价阶段。包含 `subnetx/` Python 包、`work/` 和 `tutorials/` 运行脚本、`data/` 注释和热力学相关数据、`models/` 宿主模型，以及打包配置。
- `tutorials/`: 示例运行材料分布在 `1_subnetwork_extraction/tutorials/` 和 `2_subentwork_analysis/tutorials/`。不要把教程输出误当作新的算法结果，除非任务明确要求复现教程。
- `data/`: 数据目录分布在两个阶段目录下。`1_subnetwork_extraction/data/` 主要保存 ARBRE 网络和生物体代谢物注释；`2_subentwork_analysis/data/` 主要保存模型整合、注释映射和热力学相关输入。
- `models/`: 模型文件位于 `2_subentwork_analysis/models/`。它们是运行和验证分析阶段的重要输入，不得在无明确任务时改写。

## 科学约束

- 所有新增反应必须检查元素守恒、化学计量守恒和电荷；无法确认守恒时，必须在结果中明确标注并停止将其作为可信反应使用。
- 不得未经说明改变反应方向、上下界、可逆性或交换/需求反应定义。
- 不得虚构酶、热力学数据、代谢物名称、代谢物结构、注释映射、数据库 ID 或文献支持。
- 不得用虚构数据填补缺失值；缺失值只能保持缺失、用真实来源补充，或在任务说明中显式记录处理策略。
- 不得在未经确认的情况下改变路径搜索逻辑，包括最短路径数量、前体筛选、边界代谢物处理、子网络扩展、收敛删除规则、排序目标和热力学可行性判断。

## 工程规则

- 每次代码修改必须配套测试。若当前环境无法运行测试，必须说明原因、列出计划运行的命令，并给出风险。
- 不得提交大型数据库、模型权重、运行输出、缓存、临时文件、日志、`__pycache__/`、打包产物或本地环境文件。现有历史文件仅在明确任务要求时处理。
- 不得直接修改 `main` 分支。所有开发工作应在 `baseline-reproduction` 或从其派生的任务分支上完成。
- 每个任务只解决一个明确问题；不要把格式化、重构、依赖升级、科学逻辑调整和结果生成混在同一个变更里。
- 所有运行命令和测试结果都要在回复中报告，包括工作目录、关键输出、失败信息和未运行的原因。

## 当前开发环境

- Windows 本地环境用于 Codex 阅读仓库、修改代码、整理文档和做轻量级静态检查。
- AutoDL Linux 服务器用于正式运行、数据处理、长时间任务、GPU/CPU 密集型任务和模型训练。
- GitHub 是唯一正式代码仓库；不要把本地目录、压缩包或临时副本视为正式来源。
- 当前正式远程仓库为 `https://github.com/leesonchuan/SubNetX-next`。

## 操作约定

- 修改前先确认当前分支和工作区状态；发现与任务无关的已有改动时，不要回滚，先报告。
- 只在任务明确要求时运行会生成大量输出或写入数据目录的脚本。
- 科学结果、数据处理和模型训练应优先在 AutoDL Linux 环境中复现；Windows 本地结果只能作为轻量检查，不能替代正式结果。
- 对任何数据库、模型、路径搜索或热力学相关改动，必须记录输入文件、参数、命令、随机性来源和输出位置。
